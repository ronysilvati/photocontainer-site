{% set slider_images = [url('theme://_temp/slider-tags1.png')] %}

<div id="home-slider-tags" class="carousel slide slider slider-tags vh cover" data-ride="carousel">
  <div class="carousel-inner" role="listbox">
    {% set i = 0 %}
    {% for image in slider_images %}
    <div class="carousel-item {{ ((i == 0) ? 'active' : '') }}">
      <img class="d-block img-fluid" src="{{image}}" alt="First slide">
      <div class="carousel-caption d-none d-md-block">
        <h4 class="title">No Fotocontainer você encontra suas fotosbuscando por tags</h4>
        <ul>
          <li>
            <i class="icon-location"></i>
            Clube 12 de agosto
          </li>
          <li>
            <i class="icon-location"></i>
            Clube 12 de agosto
          </li>
        </ul>
      </div>
    </div>
    {% set i = i+1 %}
    {% endfor %}
  </div>
  <a class="carousel-control-prev" href="#home-slider-tags" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#home-slider-tags" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>

</div>
